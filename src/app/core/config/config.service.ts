// this file holds all the current config of uboxit
// consider moving it outside of this application
// e.g CMS, admin-tool, apis

export class ConfigService {
  // all the configs needs to be moved here (eg pickup at store addres, category names translation)
}
